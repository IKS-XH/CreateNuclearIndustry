# 项目执行者强制规则

本文件适用于仓库根目录及其全部子目录。所有 Agent、开发者和后续执行者开工前必须阅读本文件、活动任务计划以及任务要求的 Minecraft 开发技能。

## 中文注释

- 所有新增或修改的手写代码必须使用中文编写注释和 Javadoc；类名、方法名、字段名、注册 ID、协议字段、第三方 API 名称和必要技术术语保持原文。
- 公共类型、跨包调用入口、状态模型和非显然算法必须说明职责、输入输出、单位、服务端/客户端边界以及关键不变量。
- NeoForge 生命周期、Create/Ponder 接入、网络校验、NBT 兼容、流体事务、红石状态、tick 顺序、热工公式和融毁状态机中的非显然分支必须有中文说明。
- 不给 import、花括号、普通 getter/setter 或一眼可见的赋值添加无信息注释；禁止逐行复述代码。
- 修改含英文注释的手写代码时，应把相关英文注释改为准确中文；许可证文本、第三方代码、生成文件和必须保持原样的外部协议文本除外。
- 注释必须与实际行为一致。无法确认语义时停止相关范围并向项目经理报告，不得用猜测性注释掩盖设计缺口。

详细存量注释补齐步骤见 `docs/superpowers/plans/2026-08-25-chinese-code-comments-execution-plan.md`。

## 项目治理

- Minecraft 实现任务必须使用任务计划指定的 Minecraft 开发与测试技能，并以仓库实际 NeoForge、Minecraft、Create 和 Ponder 版本为准。
- `docs/` 下的核心文档和活动计划仅允许项目经理与用户修改。其他执行者发现冲突时必须暂停并报告。
- 除项目经理和用户外，所有执行者禁止提交、回退、建分支、切换分支、合并、变基、清理工作区或改写 Git 历史。
- 执行者不得覆盖、删除、格式化或夹带任务范围外的既有改动。
