# 项目执行者强制规则

本文件适用于仓库根目录及其全部子目录。所有 Agent、开发者和后续执行者开工前必须阅读本文件、活动任务计划以及任务要求的 Minecraft 开发技能。

## 中文注释

- 所有新增或修改的手写代码必须使用中文编写注释和 Javadoc；类名、方法名、字段名、注册 ID、协议字段、第三方 API 名称和必要技术术语保持原文。
- 公共类型、跨包调用入口、状态模型和非显然算法必须说明职责、输入输出、单位、服务端/客户端边界以及关键不变量。
- NeoForge 生命周期、Create/Ponder 接入、网络校验、NBT 兼容、流体事务、红石状态、tick 顺序、热工公式和融毁状态机中的非显然分支必须有中文说明。
- 不给 import、花括号、普通 getter/setter 或一眼可见的赋值添加无信息注释；禁止逐行复述代码。
- 修改含英文注释的手写代码时，应把相关英文注释改为准确中文；许可证文本、第三方代码、生成文件和必须保持原样的外部协议文本除外。
- 注释必须与实际行为一致。无法确认语义时停止相关范围并向项目经理报告，不得用猜测性注释掩盖设计缺口。

存量中文注释专项已完成；历史执行记录见 `docs/archive/2026-08-25-chinese-code-comments-execution-plan.md`。后续代码的永久注释合同以本节和活动任务计划为准。

## 项目治理

- 用户于 `2026-09-08` 明确指定当前主持需求与规划对话的 Codex 担任项目经理。其他 Codex 对话、Agent、子 Agent 和开发者均为执行者，不因工具或模型名称相同而获得项目经理权限；角色变更只能由用户明确指定。
- 项目经理与用户共同讨论玩法设定、平衡、技术路线和实施可行性，负责把已确认需求写入文档、生成开发任务计划、审核交付、维护项目文档及管理 Git 版本。项目经理不编写或修改功能代码、测试代码、构建脚本或模拟器代码；实现和代码冲突修复交由其他执行者完成。
- 项目经理和执行者都必须实际读取并应用相关 Minecraft 技能；涉及模组技术评审、任务设计、实现和验收时，使用 `minecraft-modding` 与 `minecraft-testing`，版本与发布治理使用 `minecraft-ci-release`，其他专项按任务选用已安装技能。任务卡须记录技能入口，交付须记录实际使用情况。
- 技能示例必须与仓库实际 Minecraft、Java、NeoForge、Create 和 Ponder 版本核对；不得因技能包含新版本示例而升级技术栈或改变项目许可证、发布方式。当前技能定位与使用规则见 [项目治理与协作协议](docs/project-governance.md)。
- `AGENTS.md`、`docs/` 下的核心文档、治理协议和活动计划仅允许项目经理与用户修改。项目经理同时负责 README、工具说明、文档索引、验收归档和版本说明的一致性；执行者只能在任务卡明确允许的路径写交付报告或工具说明，不得借此改变需求。其他执行者发现冲突时必须暂停受影响范围并报告。
- 新建议与已批准需求必须区分；项目经理对用户已经明确确认的要求直接入文档，不重复索取同一授权。影响玩法含义、范围或兼容性的未决取舍先与用户讨论，未决任务不得派发为实现任务。
- 除项目经理和用户外，所有执行者禁止任何 Git 写操作，包括暂存、提交、回退、建分支、切换分支、合并、变基、挑拣提交、stash、标签写入、推送、清理工作区或直接改写 `.git`。即使为修复失败、解决冲突或满足技能中的提交步骤，也不得自行执行。
- 执行者仅可只读检查 Git 状态、差异与历史，交付未提交改动和报告；提交、回退、分支、合并及其继续/中止操作由项目经理或用户执行。Git 冲突涉及代码时，项目经理确定处理范围，由执行者修改冲突文件，项目经理审核后完成 Git 操作。
- 由项目经理或用户创建的 Git 提交必须使用中文提交信息；任务 ID、类名、注册 ID 和必要技术术语可以保留原文。
- 执行者不得覆盖、删除、格式化或夹带任务范围外的既有改动。

详细职责、技能矩阵、需求状态、任务卡模板、验收和版本流程以 [项目治理与协作协议](docs/project-governance.md) 为统一入口；活动任务仍以 [P1 单步执行计划](docs/superpowers/plans/2026-08-18-agent-developer-execution-plan.md) 为准。
