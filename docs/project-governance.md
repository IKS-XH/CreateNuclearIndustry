# 项目治理与协作协议

**生效日期：** 2026-09-08

**需求决策人：** 用户（项目所有者）

**项目经理：** 用户指定、当前主持需求与规划对话的 Codex

**适用范围：** 仓库根目录、全部子目录及本项目的其他工作区文档

本协议记录用户于 2026-09-08 确认的项目经理职责，细化 [AGENTS.md](../AGENTS.md) 和 [主策划第 13 节](./project.md#13-项目经理协作与执行规则)。其他 Codex 对话、Agent、子 Agent 和开发者默认属于执行者，不能自行取得或转授项目经理权限。用户明确变更角色后，由项目经理同步本协议和相关入口。

## 1. 角色与职责

| 事项 | 用户 | 项目经理 | Agent / 开发者 |
| :--- | :--- | :--- | :--- |
| 玩法、平衡、技术路线与范围 | 提出需求并决定未决取舍 | 共同讨论、核查依据、分析成本和可行性、记录结论 | 提供实现证据和建议，不自行改变设计 |
| 核心文档与活动计划 | 可直接修改 | 维护受影响文档、依赖、任务卡与版本记录 | 只读；在指定报告中提出修订建议 |
| 代码、测试与功能资源 | 决定需求和验收目标 | 只读审查、安排验证、反馈问题，不代写实现 | 按已确认任务卡实施、测试并报告 |
| 验收与进度 | 完成或确认需要的人工体验验收 | 审核差异与证据、必要时独立复验、更新任务状态 | 提交实际结果，不自行宣布阶段通过 |
| Git 与工作区集成 | 可执行版本操作 | 负责提交、回退、分支、合并及冲突处理编排 | 只读检查，交付未提交改动 |

项目经理不编写或修改功能代码、测试代码、构建脚本、模拟器或自动化程序；发现问题时形成可执行的任务或整改要求。项目经理可以阅读代码和依赖、运行已有构建与测试命令、检查日志及管理文档。代码或资源冲突由执行者在指定文件范围修复，文档冲突由项目经理处理，Git 合并的继续或中止仍由项目经理执行。

项目经理负责工作区内属于本项目的主策划、设计修订、内容清单、配方关系、路线图、活动计划、README、工具说明、索引、验收归档和版本说明。核心文档与活动计划只允许用户和项目经理修改；执行者只有在任务卡明确列入允许路径时，才可更新交付报告或工具说明。历史报告保留原验收语境，不通过改写历史记录追认新需求；与本项目无关的工作区文件不纳入管理性清理。

## 2. 需求讨论与落文档

1. 项目经理先说明玩家要解决的问题、预期交互、输入输出、失败与恢复方式，以及对平衡、性能、兼容和工期的影响；技术结论区分已验证事实、设计推断和待验证假设。
2. 用户已经明确提出并确认的需求直接整理入文档；同一授权不重复询问。仍有互斥玩法、重大范围或兼容性取舍时，先提出具体选项与依据，保留未决状态。
3. 讨论和评审建议不自动成为已批准需求。对整份评价的认可不等于逐项批准其中的玩法修改；未经确认的建议可登记为待讨论事项，不能写成已冻结合同或派发编码。
4. 确认后只同步实际受影响的文档，并记录日期、需求来源、结论、替代的旧规则、受影响任务和验证要求。涉及 Java、HTML 模拟器和配置的共同规则，任务计划必须列出需要同步的实现和对照验证。
5. 文档冲突先停止受影响任务的派发或执行，由项目经理整理冲突及影响，再按用户已确认的规则协调；需要新取舍时由用户决定。无关且依赖已满足的工作可以继续。

需求状态使用“待讨论、已确认、待验证、已取代”；任务状态使用“待派发、执行中、待验收、需整改、已完成、阻塞”。已确认的玩法仍可因技术假设未验证而暂不可派发，两种状态不能混为一谈。

## 3. 强制 Minecraft 技能与版本核对

项目经理必须实际使用相关技能进行技术路线与可行性评审、任务编排和验收设计；执行者必须在开工前读取任务指定的 `SKILL.md`，按当前项目版本应用，并在交付报告中列出实际使用的技能及其对应工作。只写技能名称、未读取或未应用，不满足要求。

当前已安装技能根目录为 `C:/Users/lenovo/.codex/skills/`。下表路径相对该目录，换机器或安装位置变化后，应从当前技能目录查找并记录实际路径，不把本机路径当成模组运行依赖。

| 工作类型 | 必须使用的技能 | 当前入口 |
| :--- | :--- | :--- |
| 模组技术评审、实现任务设计、注册、生命周期、网络与 Create/Ponder 集成 | `minecraft-modding` | `minecraft-modding/SKILL.md` |
| 上述模组任务的测试计划、JUnit / GameTest / 客户端与服务端验收 | `minecraft-testing`，与 `minecraft-modding` 配套 | `minecraft-testing/SKILL.md` |
| 版本规则、CI、制品与发布治理 | `minecraft-ci-release` | `minecraft-ci-release/SKILL.md` |
| 矿物、群系、维度或结构生成 | `minecraft-world-generation`，模组实现同时使用前两项 | `minecraft-world-generation/SKILL.md` |
| 纹理、模型、方块状态、语言、声音等资源专项 | `minecraft-resource-pack`；涉及模组集成与验证时配套前两项 | `minecraft-resource-pack/SKILL.md` |
| Minecraft 位图概念图、纹理概念或宣传图 | `minecraft-imagegen`，按其流程使用图像工具 | `minecraft-imagegen/SKILL.md` |

其他专项按实际任务选用已安装技能，不因技能可用就引入多加载器、服务器插件或无关系统。纯治理文档变更使用相关技能核对职责与验证边界，不因此要求运行整套游戏测试。

开工与派发前，以 `gradle.properties`、`build.gradle`、`settings.gradle` 和实际依赖确认技术基线。目前为 Minecraft `1.21.1`、Java `21`、NeoForge `21.1.219`、Create `6.0.10-280`、Ponder `1.0.82`、Flywheel `1.0.6`。技能中涉及 26.x、1.21.11、1.21.3、Forge 或 Fabric 的示例不能未经核对直接用于本项目，尤其不能据此更换 GameTest 注册方式、资源路径或 Java 版本。

用户要求和仓库当前合同优先于技能中的通用建议。技能中的提交、清理、许可证、版本后缀或发布示例不授予执行者 Git 权限，也不自动改变本项目的 `All Rights Reserved`、构建版本规则或发布范围。指定技能缺失或无法读取时，先查找实际安装入口；仍不可用则报告并暂停依赖它的工作，不宣称已经使用。

## 4. 可派发任务卡

活动任务维护在 `docs/superpowers/plans/`，由 [实施路线图](./implementation-roadmap.md) 索引；当前实现入口为 [P1 单步执行计划](./superpowers/plans/2026-08-18-agent-developer-execution-plan.md)。每张卡只交付一个可以独立验收的结果，不能把后续阶段或未批准建议顺带交给执行者。

新任务使用下列字段；已有任务卡的范围和前置条件继续有效，派发时补齐适用技能和实际基线信息。

```text
任务 ID / 标题：
状态 / 执行者：
需求来源 / 确认日期 / 权威文档段落：
唯一目标 / 玩家可观察结果：
前置任务 / 派发门 / 未决项：
分支与基准提交 / 既有未提交改动：
必读文档 / 必须使用的技能及 SKILL.md 实际路径：
实际 Minecraft / Java / NeoForge / Create / Ponder 版本：
允许修改的精确文件或目录 / 禁止修改范围：
行为合同 / 服务端与客户端边界 / 输入输出及单位：
正常场景 / 失败与恢复场景 / 守恒或状态不变量：
注册、NBT、配置、网络及模拟器兼容影响：
自动测试命令与通过标准 / 游戏内人工步骤与预期：
中文注释与资源要求：
交付报告路径 / 完成定义 / 后续依赖：
Git 约束：仅允许只读检查，禁止暂存、提交、回退、分支、合并等写操作。
```

并行派发必须满足前置已经验收、修改文件范围互不冲突；涉及共享状态或同一文件时由项目经理安排顺序。执行者遇到设计缺口、越界修改需要或代码冲突时报告具体位置和建议，不自行改需求、切分支、回退或合并。

## 5. 交付与验收

执行者提交未提交改动和指定路径中的报告，列出任务 ID、基准提交、实际技能入口、修改文件、行为变化、验证命令与结果、人工验收、兼容影响、未实现项及下一依赖。报告中的“提交”指交付报告，不授予执行 Git commit 的权限。

项目经理核对文件范围、中文注释、行为与文档一致性，再审查测试证据，必要时运行已有命令独立复验。逻辑验证、真实 Create 管网 / 机械臂集成、客户端操作体验与多人行为各自提供相应证据，单元测试或构建成功不能替代其他验收。历史报告必须标出时间和基线，不能冒充本次运行结果。

通过后由项目经理更新活动计划和路线图、处理归档并完成适用的版本操作；未通过则形成明确整改项交回执行者，项目经理不代写修复。纯文档变更核对差异范围、链接、术语和一致性，并执行 `git diff --check`；无运行行为变化时不额外跑 Minecraft 测试。

## 6. Git 与工作区管理

- 只有用户和项目经理可以执行 Git 写操作。执行者禁止 `add`、`commit`、`reset`、`restore`、`revert`、`checkout`、`switch`、分支创建/修改、`merge`、`rebase`、`cherry-pick`、`stash`、`clean`、标签创建/修改和 `push`，也不得直接修改 `.git`、通过 GUI 或脚本绕过该规则。只读的 `status`、`diff`、`log`、`show` 和版本查询可用于报告。
- 项目经理在操作前检查当前分支、基准提交、暂存区、未提交改动和执行者活动范围。只暂存本次已经审核的文件或片段，不使用 `git add .` 夹带日志、存档、生成物或其他任务改动。
- 新建工作分支默认使用 `codex/` 前缀；保留已有分支语义。提交按可追踪的任务或文档变更组织，提交信息使用中文，必要任务 ID 和技术标识保持原文。
- 回退优先使用可追踪的 `git revert`；回退前确认目标提交、受影响任务与用户已有改动，不用 `reset --hard`、强制检出或清理命令掩盖失败。尚未授权且会丢弃用户工作或改写已共享历史的操作，必须先向用户说明具体影响并取得授权；已授权范围不重复询问。
- 合并前确认来源与目标分支、相关测试和文档状态，并使用适合该合并的干净工作区。存在无关未提交改动时保留它们，选择隔离工作区或推迟合并，不擅自删除或收纳用户工作。代码冲突交执行者处理，项目经理审核后继续或中止 Git 流程。
- 项目经理维护版本说明、兼容影响与已知限制；发布制品和远端推送遵循用户已经确认的范围与仓库发布方式，不因本协议自动新增发布平台、流水线或公开版本。
- 文件权限或执行环境阻止 Git 写入时，保留已完成文档和代码，报告具体阻塞及尚未执行的版本操作，不绕过权限、不宣称提交成功。职责授权与工具是否具备写入能力分别记录。

## 7. 本次生效记录

本次仅确认并落实项目经理职责、执行者权限、技能使用和文档/任务/版本管理流程；没有批准上一轮评审提出的损伤、融毁、燃耗、自动化或输出平衡修改。后续讨论形成具体需求后，再由项目经理同步设计并编排实现任务。

当前 P1 仍按既有计划推进；`P1-MELTDOWN-02` 的事故产物表冻结门继续有效，本协议不替代任何玩法未决项或既有验收记录。
