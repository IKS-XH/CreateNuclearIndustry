# P1-GOGGLE-INSTRUMENT-04 护目镜动态显示验收归档

日期：2026-08-31

## 验收结论

用户在客户端人工测试后确认：全部全堆与逐列信息集中在仪表端口的同一个护目镜窗口中难以阅读，因此正式采用现场分层显示。仪表端口负责全堆总览，燃料列和控制棒列详情分别在对应换料端口与控制棒驱动器就地显示。该决策属于用户批准的交互合同变更，当前实现不需要把逐列信息重新塞回仪表端口。

项目经理审查只读同步、显示归属、确定性格式、结构失效和等待态后确认任务完成。执行者未补交原计划路径下的 `build/reports/p1/P1-GOGGLE-INSTRUMENT-04.md`，本文件由项目经理依据源码、自动回归与用户人工验收形成最终验收记录。

## 正式显示归属

| 观察目标 | 护目镜显示内容 |
| --- | --- |
| `reactor_instrument_port` | 静态结构摘要；冷/热冷却剂当前库存与配置容量；全堆新生裂变发热量；实际冷却剂转化速率 |
| `reactor_refueling_port` | 绑定燃料列的 1-based 行列编号、完整度百分比和新生裂变发热量；燃料耐久与派生损坏度仍由后续完整换料端口护目镜任务补齐 |
| `control_rod_drive` | 绑定控制棒列的 1-based 行列编号和完整度百分比 |

仪表端口不枚举逐列详情，避免窗口过长；空列不伪造成燃料列或控制棒列。三类显示均只读取服务端已同步的客户端副本，不在客户端扫描结构、运行模拟、轮询服务端或修改权威快照。

## 自动验收

- `./gradlew.bat test --rerun-tasks`：176/176 JUnit 通过，0 失败、0 错误、0 跳过。
- `./gradlew.bat runGameTestServer --rerun-tasks`：首次运行在 NeoForge 原生 `GameTestInfo` 的 fastutil 迭代器中发生瞬态框架崩溃，尚未产生具体测试失败；立即重跑后 47/47 required GameTest 通过。
- `git diff --check`：通过。

覆盖内容包括：三类方块的字段归属、仪表端口不枚举逐列信息、1-based 行列编号、固定一位小数、百分比与 `HU/t`/`mB/t` 单位、零库存、零发热、零转化、首次正式 tick 前等待态、客户端更新包、结构扫描计数不增长以及护目镜读取不改变权威快照。

## 人工验收

用户于 2026-08-31 在客户端确认当前分层显示方案通过，并明确说明该调整是为解决“所有信息集中在一个窗口里看不过来”的可读性问题。该人工结论覆盖实际 Create 工程师护目镜回调和窗口可读性。

非零冷/热库存和实际转化速率经真实 Create 储罐—机械泵—管道链路的人工验证继续由 `P1-COOL-04` 完成，不反向阻塞本任务。

## 后续边界

- 下一任务为 `P1-COOL-04`，证明成型冷端口可通过真实 Create 管网输入冷却剂，并验证非零库存、冷转热和热端输出。
- `P1-GOGGLE-01/02` 仍需补齐换料端口的燃料组件剩余耐久和派生损坏度；不得把本任务已有的完整度与发热显示误当作全部换料端口信息已经完成。
- 不修改服务端模拟公式、端口流体能力、结构绑定或遥测状态所有权。
