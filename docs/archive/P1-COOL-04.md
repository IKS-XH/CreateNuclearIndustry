# P1-COOL-04 验收归档：真实 Create 管网冷却剂输入

**验收日期：** 2026-08-31
**状态：** 已完成
**后续门禁：** `P1-THERMAL-01`

## 1. 验收结论

成型反应堆的正式冷端 capability 可以被 Create 6.0.10 的储罐、获得动力且方向正确的机械泵和流体管网发现并调用。输入写入仪表端口持有的唯一冷库存；结构失效后 capability 消失，修复重扫后恢复。反向泵和错误流体不能修改库存。

本任务没有发现需要修改生产端口或 Create 兼容层的缺陷，交付内容为真实管网 GameTest 和报告。用户随后完成客户端人工验收，确认真实输入、运行转化与热端输出链路通过。

## 2. 验证证据

- 执行者报告：`build/reports/p1/P1-COOL-04.md`。
- 项目经理独立执行 `./gradlew.bat test --rerun-tasks --max-workers=1`：181 项 JUnit，0 失败、0 错误、0 跳过。
- 项目经理独立执行 `./gradlew.bat runGameTestServer --rerun-tasks --max-workers=1`：最终 51 个 required GameTest 全部通过。
- GameTest 首次运行触发 NeoForge 1.21.1 `GameTestInfo`/fastutil 迭代器瞬态框架崩溃，没有模组断言失败；第二次相同命令完整通过。
- 用户完成客户端人工验收。

## 3. 后续发现

人工运行反应堆时又发现一项既有模拟层问题和一项需要区分口径的运行现象，它们不属于本任务的 Create 管网接入范围：

1. 整数 `mB` 转化留下的亚毫桶热量余数可能超过完整度损伤阈值，导致供给和热端空间充足时仍偶发扣除完整度；由 `P1-THERMAL-01` 处理。
2. 当时测试布局是孤立燃料列，后续 `P1-THERMAL-01` 已确认完全插棒后的新生裂变热归零、缓存余热继续守恒冷却。此后另用三行 `F-C-F` 六燃料布局确认正式 Java 反馈超频错误地绕过控制棒，该独立缺陷由 `P1-CONTROL-05` 处理。

这些后续问题不推翻真实 Create 管网已经可用的验收结论。
